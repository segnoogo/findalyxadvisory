# Normalise les fins de ligne en LF dans le depot (portable, stable pour GitHub Pages)
* text=auto eol=lf

# Fichiers binaires : jamais de conversion
*.png binary

# Le script d'assemblage doit rester en LF (shebang bash)
*.sh text eol=lf
